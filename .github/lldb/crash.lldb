# When this executes, we've already done the coredump. We need to kill the process in a way that allows teardown.lldb to exit with the right exit code.
script -l python -- import lldb; import os; import sys; lldb.process.Kill(); sys.exit(lldb.process.exit_state)
kill
